package com.bloom.app.domain.enums;

public enum MovementSourceType {
    OPENING_BALANCE,
    SALE,
    STOCK_ADJUSTMENT,
    GOODS_RECEIPT,
    GOODS_RECEIPT_CANCELLATION,
    STOCK_OPNAME,
    PURCHASE,
    RETURN,
    TRANSFER
}

package com.bloom.app.domain.model;

/** Stable Release 1 unit codes. Persist enum names; do not rename existing values. */
public enum UnitOfMeasure {
    PIECE,
    KILOGRAM,
    GRAM,
    LITER,
    MILLILITER,
    METER,
    CENTIMETER,
    ROLL
}
